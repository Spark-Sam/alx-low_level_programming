#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: All single digits
 *
 * Return: 0
 */
int main(void)
{
	int digit;

	for (digit = -1; digit < 9; digit++)
	{
		printf("%d\n", digit);
	}

	return (0);
}
